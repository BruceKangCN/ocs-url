src += qt.compile_resources(
  sources: ['desktop.qrc'],
)

install_data(
  'ocs-url.desktop',
  install_dir: 'applications',
)

install_data(
  'ocs-url.svg',
  install_dir: 'icons' / 'hicolor' / 'scalable' / 'apps',
)
